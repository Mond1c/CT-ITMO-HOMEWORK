#include <bits/stdc++.h>
using namespace std;



int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    int n, m, k;
    cin >> n >> m >> k;
    vector<string> strings(n);
    for (int i = 0; i < n; ++i) {
        cin >> strings[i];
    }
    for (int i = 0; i < m; ++i) {
         
    }
    return 0;
}